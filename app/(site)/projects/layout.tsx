export const metadata = {
  title: 'Projects',
  description: 'Full applications and experiments built with AI-first development',
}

export default function ProjectsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}